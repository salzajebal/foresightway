# IPO image backups

Daily off-site archives from `/var/lib/foresightway/ipo-images`.
Archives older than 30 days are removed automatically.
Verify an archive with its adjacent SHA-256 file before restoring it.
